import 'package:flutter/widgets.dart';

class ProjectionsPage extends StatelessWidget {
  const ProjectionsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("Projections");
  }
}
