import 'package:flutter/widgets.dart';

class HallsPage extends StatelessWidget {
  const HallsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("Halls");
  }
}
