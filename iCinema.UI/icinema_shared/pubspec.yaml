name: icinema_shared
description: Shared code package for iCinema desktop and mobile applications
version: 1.0.0+1
publish_to: 'none'

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter
  
  # HTTP Client
  dio: ^5.8.0
  
  # JSON Serialization
  json_annotation: ^4.9.0
  
  # Equatable for value comparison
  equatable: ^2.0.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  build_runner: ^2.6.0
  json_serializable: ^6.10.0

